gremlin.graph=org.apache.hugegraph.HugeFactory

backend=eloq
serializer=binary

store=hugegraph

# task config
task.scheduler_type=local
task.schedule_period=10
task.retry=0
task.wait_timeout=300

# search config
search.text_analyzer=jieba
search.text_analyzer_mode=INDEX
